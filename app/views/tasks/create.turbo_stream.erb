<%= turbo_stream.prepend 'tasks', partial: 'tasks/task', locals: { task: @task } %>
<%= turbo_stream.remove 'tasks_empty' %>
<%= turbo_stream.replace 'new_task', partial: 'tasks/new_button' %>
<%= turbo_stream.replace 'flash', partial: 'shared/flash' %>
